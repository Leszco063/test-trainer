// Zusätzliche, automatisch erzeugte Aufgaben für die Prüfungsabschnitte.
// Wird mit den Aufgaben-Generatoren erweitert (Figuren, Rechnen, Diagramme).

export function examExtras(section, used) {
  return [];
}
